require 'spec_helper'

describe Perfil do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Perfil.create!(@valid_attributes)
  end
end
